#pragma once
#include "glm/glm.hpp"

namespace glm_ext {
	glm::vec2 new_vec2(float x, float y);
	glm::vec3 new_vec3(float x, float y, float z);
	glm::vec4 new_vec4(float x, float y, float z, float w);
}